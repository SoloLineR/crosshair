export type CrosshairSettingsObj = {
  dotOpacity: number; // Opacity of the center dot (0.0 - 1.0)
  dotOutlineOpacity: number; // Opacity of the dot outline (0.0 - 1.0)
  pipBorder: boolean; // Whether the pip has a border
  pipGap: number; // Gap between pips and center dot
  pipHeight: number; // Height of the pips
  pipOpacity: number; // Opacity of the pips (0.0 - 1.0)
  pipWidth: number; // Width of the pips
  colorR: number; // Red component of crosshair color (0-255)
  colorG: number; // Green component of crosshair color (0-255)
  colorB: number; // Blue component of crosshair color (0-255)
};
